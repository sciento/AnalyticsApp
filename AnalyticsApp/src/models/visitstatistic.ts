namespace AnalyticsApp {

    export namespace Model {

        export interface VisitStatistic {
            site: Site;
            averageVisitTime: number;
            visits: number;
        }
    }
}
